<Window
  x:Class="Taskmanager2.App.Views.Shell"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="Taskmanager2">

  <Window.SystemBackdrop>
    <MicaBackdrop />
  </Window.SystemBackdrop>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Real Win11 TM title bar: the search box sits IN the title bar (ExtendsContentIntoTitleBar,
         wired in code-behind). Only DragRegionLeft/DragRegionRight are the drag surface, so the
         search box stays clickable; right side keeps clear of the system caption buttons. -->
    <Grid Grid.Row="0" x:Name="AppTitleBar" Height="48" ColumnSpacing="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="140" />
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0" Margin="14,0,0,0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
        <FontIcon FontSize="14" Glyph="&#xE9D9;" />
        <TextBlock FontSize="12" VerticalAlignment="Center" Text="Taskmanager2" />
      </StackPanel>
      <Border Grid.Column="1" x:Name="DragRegionLeft" Background="Transparent" />
      <TextBox
        Grid.Column="2"
        x:Name="GlobalSearch"
        Width="420"
        VerticalAlignment="Center"
        PlaceholderText="Geben Sie zum Suchen einen Namen, einen Herausgeber oder eine PID ein"
        Text="{x:Bind ProcessList.Filter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
      <Border Grid.Column="3" x:Name="DragRegionRight" Background="Transparent" />
      <!-- Column 4 stays empty: reserved for the system caption buttons (Min/Max/Close). -->
    </Grid>

    <!-- First-run install hint: shown once at startup when the PawnIO driver is missing. -->
    <InfoBar
      Grid.Row="1"
      IsClosable="True"
      IsOpen="{x:Bind BannerViewModel.ShowBanner, Mode=OneWay}"
      Message="{x:Bind BannerViewModel.BannerText, Mode=OneWay}"
      Severity="Informational" />

    <NavigationView
      Grid.Row="2"
      x:Name="Nav"
      IsBackButtonVisible="Collapsed"
      IsSettingsVisible="True"
      PaneDisplayMode="LeftCompact"
      IsPaneToggleButtonVisible="True"
      SelectionChanged="OnSelectionChanged">
      <NavigationView.MenuItems>
        <NavigationViewItem Content="Prozesse" Tag="Prozesse">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE9D9;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="Leistung" Tag="Leistung">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE9D2;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="App-Verlauf" Tag="AppVerlauf">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE81C;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="Autostart-Apps" Tag="Autostart">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE7E8;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="Benutzer" Tag="Benutzer">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE77B;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="Details" Tag="Details">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE946;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="Dienste" Tag="Dienste">
          <NavigationViewItem.Icon>
            <FontIcon Glyph="&#xE90F;" />
          </NavigationViewItem.Icon>
        </NavigationViewItem>
      </NavigationView.MenuItems>
      <Frame x:Name="ContentFrame" />
    </NavigationView>
  </Grid>
</Window>
